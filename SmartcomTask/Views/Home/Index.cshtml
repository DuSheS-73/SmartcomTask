@{
    ViewData["Title"] = "Home page";
    bool isAdmin = User.IsInRole("Admin");
}

<div id="app">
    <h1>Home Page</h1>
    <home-component :is-admin="@isAdmin.ToString().ToLower()" items-url="@Url.Action("Index", "Items")" customers-url="@Url.Action("Index", "Customers")"></home-component>
</div>

@section scripts{
    <script src="~/js/home.js" asp-append-version="true"></script>
}